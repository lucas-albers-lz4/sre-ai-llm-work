# Sources

Master index of every source this guide cites. Empty at MVP bootstrap — the
Miner and Smith populate this as notes merge.

| Source | Type | Notes |
|--------|------|-------|
| docs-google-sre-prodcast-01-09-postmortems | discussion | Google SRE Prodcast S1E09: postmortem definition, blamelessness, when-to-write rubric, action-item hygiene |
| docs-google-sre-reliable-data-processing-minimal-toil | docs | Google SRE paper: batch-job safety levels, staged release pipeline, population canarying, freshness/correctness SLOs |
| docs-google-sre-engagement-model | docs | Google SRE Workbook Ch18: 7-phase service-lifecycle engagement model, NYT shared-goals, "as quickly as is safe" pledge, SRE-to-developer ratio, hand-back conditions |
| docs-google-sre-incident-response | docs | Google SRE Workbook Ch9: resolve-vs-manage split, IC/CL/OL roles, declare-early, generic mitigations, mitigation-first, PagerDuty IR process and tooling, drills |
| docs-google-sre-on-call | docs | Google SRE Workbook Ch8: two-incident pager budget, pager-load anatomy, response-time tiers, gated alert introduction, staffing, shift/scheduling rules |
| docs-google-sre-postmortem-analysis | docs | Google SRE Workbook Appendix C: 68% push-driven outage triggers, root-cause taxonomy, standard-template → trend-analysis mechanism |
| docs-google-sre-reaching-beyond-walls | docs | Google SRE Workbook Ch19: platform reliability partnership, five-step customer-SRE methodology, tenant-selection frameworks |
| docs-google-sre-simplicity | docs | Google SRE Workbook Ch7: five systems-level complexity proxies, complexity-as-externality, simplification management (10% budget, celebrate code deletion, rotating whole-stack group), structured tool contracts (bag vs Protocol Buffers), shared-platform → tiered engagement |
| docs-google-sre-slo-engineering-case-studies | docs | Google SRE Workbook Ch3: Evernote/THD SLO adoption — SLI measurement design, VALET framework, TPS Reports SLI automation, error-budget prioritization, shared-SLO provider partnership, org-change mechanics |
| docs-google-sre-team-lifecycles | docs | Google SRE Workbook Ch20: three SRE principles, first-SRE hiring/embedding, Tuckman team formation, NYT in-place conversion, scaling to many teams, 1:5–1:50 staffing ratio, workload hand-back |
| blog-litellm-july-stability-update | blog-post | LiteLLM July stability update: MCP credential-resolver refactor (declare-don't-infer, fail-closed typed match), pass-through memory streaming, 134-fix by-area breakdown, 95% e2e coverage goal |
| blog-litellm-version-support | blog-post | LiteLLM rolling release-support window: four most recent stable minor lines effective 2026-06-29, ~1 month coverage per line, pin/patch vs line-move operator guidance, enterprise and high-severity exceptions |
| docs-langfuse-agent-skill | docs | Langfuse vendor docs: the shipped Agent Skill — a SKILL.md + references/ folder implementing the open Agent Skills standard, progressive disclosure, install surfaces (skills CLI, Cursor plugin, clone+symlink), and the allowed-tools frontmatter allowlist |
| docs-langfuse-alerts | docs | Langfuse vendor docs: the shipped Alerts feature — score/metric-driven alert conditions, two-threshold ladder, severity state machine with per-transition notify semantics, no-data modes, renotify, delivery-failure circuit breaker, HMAC webhook schema, GitHub Actions workflow_dispatch channel |
| docs-langfuse-cli | docs | Langfuse vendor docs: the CLI wrapping the full OpenAPI-generated API — machine-readable exit-code taxonomy (usage/config/network/HTTP/local), env-var-only auth shared with the SDKs, editor-agent and CI/CD scripting use cases |
| failure-litellm-vllm-embeddings-encoding-format | failure-report | LiteLLM incident: explicit `encoding_format=None` broke vLLM embeddings ~3h while OpenAI stayed green; filter-to-omit fix and per-backend exact-payload tests |
| docs-litellm-a2a-agent-gateway | docs | LiteLLM Agent Gateway (A2A overview): six-runtime agent registration, `X-LiteLLM-Trace-Id`/`X-LiteLLM-Agent-Id` forwarding obligation, per-agent authz → 403, trace-ID enforcement → 400, per-method routing split (`tasks/*` bypass the client path) |
| docs-litellm-a2a-agent-permissions | docs | LiteLLM A2A agent permissions: fail-open default (unconfigured = all agents), key∩team intersection, union-then-intersect order, dashboard-only `agent_access_groups` (no API field), narrower than MCP's permission depth |
| docs-litellm-a2a-agent-card | docs | LiteLLM A2A agent card: curated reduced card, signatures/`AgentCardSignature` dropped, per-skill `securityRequirements` dropped, `metadata.skillId` forwarded unenforced, manual re-sync drift |
| docs-litellm-a2a-invoking-agents | docs | LiteLLM A2A invocation paths: OpenAI-compatible `model: a2a/<name>` bridge collapses task state/artifacts, docs silent on bridge instrumentation, a2a-sdk 1.x breaking client migration |
| docs-litellm-a2a-iteration-budgets | docs | LiteLLM per-session agent-loop caps: `max_iterations`/`max_budget_per_session`, fail-open without outbound trace flag, accumulate-after/check-before, 429 `budget_exceeded`, TTL-windowed counters, storage-model self-contradiction |
| docs-litellm-adaptive-router | docs | LiteLLM standalone adaptive router: satisfaction-signal bandit over 7 request types, Postgres-persisted state forgotten on restart, `/state` `samples` as learning-progress check, bounded learning (no latency term, 200 obs/cell, no decay) |
| docs-litellm-streaming-token-usage | docs | LiteLLM streaming usage contract: `stream_options={"include_usage": True}` opt-in, totals in one final chunk with empty `choices`, `usage: null` on all other chunks |
| docs-litellm-token-usage-helpers | docs | LiteLLM `token_counter`/`cost_per_token`/`completion_cost`: local estimators over the bundled `model_cost` map with no provider-side reconciliation |
| docs-litellm-anthropic-count-tokens | docs | LiteLLM `/v1/messages/count_tokens`: pre-flight, provider-routed token counting, `vertex_count_tokens_location` constraint, free-but-logged (Cost Tracking ❌ / Logging ✅), local-tiktoken fallback with proxy concurrency bounds |
| docs-litellm-anthropic-unified | docs | LiteLLM `/v1/messages` unified endpoint: single-switchboard feature matrix, non-streaming-only guardrail carve-out, fallbacks/loadbalancing scoped to supported models, response-side cache-token accounting |
| docs-litellm-apply-guardrail-endpoint | docs | LiteLLM `/guardrails/apply_guardrail`: out-of-band guardrail invocation, block-as-free-text-`detail` with no documented status, client-supplied `metadata` policy channel, auth-scoping and failure-knob gaps |
| docs-litellm-audio-transcription | docs | LiteLLM `/v1/audio/transcriptions`: `mock_testing_fallbacks` stripped from proxy requests as of v1.85.0, induced-provider-error replacement, `mode: audio_transcription` registration, non-streaming-only guardrail carve-out |
| docs-litellm-benchmarks | docs | LiteLLM gateway benchmarks: v1.101.0 high-throughput profile (nightly, 33×4 vs 132×1 workers), per-component A/B deltas, client-vs-gateway success split (92.07% vs 100%), Little's Law think-time rule, `network_mock` harness |
| docs-litellm-caching-hosted-cache | docs | LiteLLM hosted response cache (`Cache(type="hosted")` → api.litellm.ai): vendor-side payload placement with no documented TTL/retention/residency contract, asynchronous cache write on the streaming path |
| docs-litellm-gateway-auth-reference | docs | LiteLLM gateway auth reference: MCP ASGI routes bypass vendor auth aliases and `x-litellm-tags`, MCP's nine-value `auth_type` enum vs A2A's inferred mode, MCP-only `mcp_jwt_signer` zero-trust guardrail |
| docs-promptfoo-assertions-metrics | docs | promptfoo assertions hub: weighted-average scoring, `threshold: 0` and `weight: 0` silent-pass traps, `assert-set` grouping, silent-0 derived metrics, trace/trajectory family, post-hoc `--model-outputs` replay |
| docs-promptfoo-model-graded-metrics | docs | promptfoo model-graded hub: ambient (unpinned) grader selection, grader-override precedence and shorthand `config`-inheritance trap, `showThinking` scratchpad misparse, score-blind `llm-rubric` pass, `agent-rubric`/`search-rubric` |
| docs-promptfoo-model-graded-context-faithfulness | docs | promptfoo `context-faithfulness`: supported-claims/total-claims score, `threshold` default 0, `query`+`context` contract, "May miss implicit claims" false-negative bound |
| docs-promptfoo-model-graded-context-recall | docs | promptfoo `context-recall`: attributable-statements/total-statements score (retrieval completeness, mirror of faithfulness), `threshold` default 0, `value`+`context` contract, "Requires known correct answers" hard bound |
| docs-promptfoo-conversation-relevance | docs | promptfoo `conversation-relevance`: windowed-proportion score, documented `threshold` default 0.5, short-conversation degenerate case, literal (non-Nunjucks) `_conversation` fixture contract |
| docs-promptfoo-answer-relevance | docs | promptfoo `answer-relevance`: two overridable provider slots (question-generation LLM + embedding model), non-deterministic by construction, threshold rebasing on embedding-model swap |
| docs-promptfoo-deterministic-metrics | docs | promptfoo deterministic assertion catalog: vendor-drawn deterministic/model-graded boundary, "deterministic ≠ hermetic", cache coupling of cost/latency gates, fail-closed `is-xml`/`tool-call-f1`, trace assertions throw when traces are absent |
| docs-promptfoo-javascript-assertions | docs | promptfoo `javascript` assertions: `boolean|number|GradingResult` contract, throw-means-fail, `not-javascript` pre-inversion scoring, `context.trace` surface, fail-open `if (!context.trace) return true` guard, broken vendor example |
| docs-promptfoo-guardrails-assertions | docs | promptfoo `guardrails`: verdict-reader (not a runner), omitted-field fail-open at score 1, `purpose: redteam` force-pass hiding detect-only bypasses, per-endpoint normalization matrix, fail-closed custom-target contract |
| docs-promptfoo-classifier-grading | docs | promptfoo `classifier`/`not-classifier`: per-label `value`+`threshold`, Hub-repo ≠ hosted inference, archived flagship injection detector, label-bound non-portable thresholds, `defaultTest.options.provider` global rule |
